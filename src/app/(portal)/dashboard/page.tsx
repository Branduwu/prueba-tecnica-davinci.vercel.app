import Link from 'next/link'; import { requireAdmin } from '@/lib/auth'
const money=(n:number)=>new Intl.NumberFormat('es-MX',{style:'currency',currency:'MXN'}).format(n||0)
export default async function Dashboard(){const db=await requireAdmin();const now=new Date();const today=now.toISOString().slice(0,10);const startWeek=new Date(now.getTime()-6*864e5).toISOString();const month=new Date(now.getFullYear(),now.getMonth(),1).toISOString(); const [todaySales,weekSales,monthSales,expenses,low]=await Promise.all([db.from('sales').select('total').gte('created_at',today),db.from('sales').select('total').gte('created_at',startWeek),db.from('sales').select('total').gte('created_at',month),db.from('expenses').select('amount').gte('expense_date',month.slice(0,10)),db.from('products').select('id',{count:'exact',head:true}).filter('stock','lte','low_stock_threshold')]);const sum=(rows:({total?:number;amount?:number}[]|null),key:'total'|'amount')=>(rows??[]).reduce((a,r)=>a+Number(r[key]??0),0);const sales=sum(monthSales.data,'total'),out=sum(expenses.data,'amount');return <><div className="metrics"><Metric label="Ventas hoy" value={money(sum(todaySales.data,'total'))}/><Metric label="Esta semana" value={money(sum(weekSales.data,'total'))}/><Metric label="Este mes" value={money(sales)}/><Metric label="Egresos" value={money(out)}/><Metric label="Flujo neto" value={money(sales-out)}/><Metric label="Stock bajo" value={String(low.count??0)}/></div><section className="card"><h3>Accesos rápidos</h3><div className="quick"><Link href="/pos">Abrir POS</Link><Link href="/inventario">Gestionar inventario</Link><Link href="/finanzas">Ver finanzas</Link></div></section></>}
function Metric({label,value}:{label:string;value:string}){return <article className="metric"><span>{label}</span><strong>{value}</strong></article>}
